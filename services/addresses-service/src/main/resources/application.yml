spring:
  application:
    name: addresses-service
  datasource:
    url: ${ADDRESSES_DATABASE_URL:jdbc:postgresql://localhost:5432/addresses_service}
    username: ${ADDRESSES_DATABASE_USERNAME:addresses_service}
    password: ${ADDRESSES_DATABASE_PASSWORD:addresses_service}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  cloud:
    discovery:
      enabled: ${SPRING_CLOUD_DISCOVERY_ENABLED:false}
  main:
    banner-mode: "off"

server:
  port: ${ADDRESSES_SERVICE_PORT:8084}

logging:
  level:
    root: info

management:
  endpoints:
    web:
      exposure:
        include: health,info
  tracing:
    enabled: ${MANAGEMENT_TRACING_ENABLED:false}
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}

eureka:
  client:
    enabled: ${EUREKA_CLIENT_ENABLED:false}
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
    register-with-eureka: ${EUREKA_REGISTER_WITH_EUREKA:true}
    fetch-registry: ${EUREKA_FETCH_REGISTRY:true}
  instance:
    prefer-ip-address: true
